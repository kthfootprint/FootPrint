import React from "react";

const SearchError = () => {
  return (
    <div className="infoView">
      Sorry, but that search query did not yield any results. Please try
      something else.
    </div>
  );
};

export default SearchError;
